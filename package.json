{
  "name": "hi",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check",
    "collect": "tsx collector/index.ts",
    "probe": "tsx scripts/imap-probe.ts",
    "test:extract": "tsx scripts/test-extract.ts",
    "test:collect": "tsx scripts/test-collect.ts",
    "deploy": "astro build && wrangler deploy -c dist/server/wrangler.json",
    "db:create": "wrangler d1 create financing-invoices",
    "db:migrate": "wrangler d1 migrations apply financing-invoices --remote",
    "db:migrate:local": "wrangler d1 migrations apply financing-invoices --local"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.111.0",
    "@astrojs/cloudflare": "^14.1.2",
    "astro": "^7.0.0",
    "fast-xml-parser": "^5.9.3",
    "imapflow": "^1.4.6",
    "mailparser": "^3.9.14"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@cloudflare/workers-types": "^4.20260702.1",
    "tsx": "^4.23.0",
    "typescript": "^6.0.3"
  }
}
